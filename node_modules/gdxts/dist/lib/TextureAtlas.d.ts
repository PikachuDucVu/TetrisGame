import { Texture } from './Texture';
import { TextureRegion } from './TextureRegion';
import { Disposable } from './Utils';
export declare class TextureAtlas implements Disposable {
    pages: Texture[];
    regions: TextureRegion[];
    constructor(pages: Texture[], regions: TextureRegion[]);
    dispose(): void;
    getPages(): Texture[];
    getRegions(): TextureRegion[];
    findRegion(name: string, index?: number): TextureRegion | null;
    findRegions(name: string): TextureRegion[];
    static load(gl: WebGLRenderingContext, packFileUrl: string, textureOptions?: any): Promise<TextureAtlas>;
}
