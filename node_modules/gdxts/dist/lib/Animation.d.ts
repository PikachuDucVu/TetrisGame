import { TextureRegion } from './TextureRegion';
export declare enum PlayMode {
    NORMAL = 0,
    LOOP = 1,
    LOOP_PINGPONG = 2,
    REVERSED = 3,
    LOOP_REVERSED = 4
}
export declare class Animation {
    private keyFrames;
    private frameDurations;
    private totalDuration;
    constructor(keyFrames: TextureRegion[], frameDurations: number | number[]);
    getFrameNumber(stateTimeRemainder: number, reverse?: boolean): number;
    getKeyFrame(stateTime: number, mode: PlayMode): TextureRegion;
}
