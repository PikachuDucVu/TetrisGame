import { Matrix4 } from './Matrix4';
import { Vector2 } from './Vector2';
import { Vector3 } from './Vector3';
export declare class OrthoCamera {
    position: Vector3;
    direction: Vector3;
    up: Vector3;
    near: number;
    far: number;
    zoom: number;
    viewportWidth: number;
    viewportHeight: number;
    projectionView: Matrix4;
    inverseProjectionView: Matrix4;
    projection: Matrix4;
    view: Matrix4;
    combined: Float32Array;
    screenWidth: number;
    screenHeight: number;
    constructor(viewportWidth: number, viewportHeight: number, screenWidth: number, screenHeight: number);
    setYDown(yDown: boolean): void;
    resize(viewportWidth: number, viewportHeight: number, screenWidth: number, screenHeight: number): void;
    setPosition(cameraX: number, cameraY: number): void;
    update(): void;
    screenToWorld(screenCoords: Vector3, screenWidth?: number, screenHeight?: number): Vector3;
    worldToScreen(worldCoords: Vector3, screenWidth?: number, screenHeight?: number): Vector3;
    tmp3: Vector3;
    unprojectVector2(worldCoord: Vector2, screenCoord: Vector2): void;
    setViewport(viewportWidth: number, viewportHeight: number): void;
}
