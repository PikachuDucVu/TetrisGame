import { OrthoCamera } from './Camera';
import { InputEvent, InputHandler } from './InputHandler';
import { Vector2 } from './Vector2';
import { Viewport } from './Viewport';
export declare class ViewportInputHandler {
    viewport: Viewport;
    canvas: HTMLCanvasElement;
    inputHandler: InputHandler;
    screenCoord: Vector2;
    worldCoord: Vector2;
    constructor(viewport: Viewport);
    isTouched(): boolean;
    cleanup(): void;
    addEventListener(event: InputEvent, listener: (x: number, y: number) => void): void;
    getX(): number;
    getY(): number;
    getTouchedWorldCoord(camera?: OrthoCamera): Vector2;
}
